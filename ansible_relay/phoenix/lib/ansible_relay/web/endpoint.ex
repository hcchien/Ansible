defmodule AnsibleRelay.Web.Endpoint do
  @moduledoc """
  Thin module for future Phoenix.Endpoint migration (Q2/Q3).

  In Q1 the Bandit server is started directly in Application.start/2,
  with AnsibleRelay.Web.Router as the Plug. This module is a placeholder
  so the directory structure mirrors the intended Phoenix layout.
  """
end
