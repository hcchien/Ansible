name: ansible_domain
description: Ansible core business logic (boards, threads, posts, sync, VC, policy)
version: 0.1.0
environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.7.0
  http: ^1.1.0
  equatable: ^2.0.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.21.0
