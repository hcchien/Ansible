-- Databases for the docker-compose dev stack (postgres image runs this once
-- on first boot of the data volume).
CREATE DATABASE ansible_relay;
CREATE DATABASE ansible_appview;
CREATE DATABASE ansible_issuer;
