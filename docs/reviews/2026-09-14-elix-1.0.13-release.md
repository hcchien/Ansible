# Elix 1.0.13 release

Date: 2026-09-14. User authorized pushing the completed product-flow fixes to prod and uploading 1.0.13 to TestFlight.

## Source and scope

- Product changes: `2b3ad8c`; version commit and deployed source: `f7ee5d583918e9f96e3429937a1dab001abdc01f`.
- `origin/prod` advanced normally from `a846fb40` to `f7ee5d58`; unrelated dirty changes in the original prod checkout were preserved.
- Version: **1.0.13 (2026091401)**, bundle ID `com.reviz.elix`.
- Scope and test limitations: [PD1–PD8 implementation report](2026-09-14-product-flow-implementation.md). The nine previously failing Flutter test files are still a known baseline limitation; this release does not claim a completely green Flutter suite.

## Constitution Review

This release uses the previously reviewed Relay/public-reading architecture and production configuration. Public/private boundaries, explicit publication and local key custody remain unchanged. No new client-to-Viewer checkpoint or Viewer proxy was introduced. TestFlight upload is separate from App Store review submission.

## Production CI/CD — completed

| Service | Cloud Build | Result | Ready revision |
|---|---|---|---|
| Relay | `5915bf86-a109-4f8c-a6a5-d6ecb276916f` | SUCCESS, including tests and migration | `ansible-relay-prod-00039-lrg` |
| AppView | `5a2fb6ae-8f36-4931-9667-22b5894b70cf` | SUCCESS | `ansible-appview-prod-00016-vlw` |
| Web | `a64b4346-7903-42a6-9db9-819343622ace` | SUCCESS | `ansible-web-prod-00038-qs4` |

Production smoke checks returned HTTP 200 for Relay explore, timeline, thread and search, plus the same standalone content identity through Relay, AppView and the Web proxy. The sampled Relay page returned 43 public, verified items; 24 note/murmur identities overlapped the AppView sample. Filtering can return fewer items than the scanned-page limit; cursors retain pagination progress.

Warm Relay explore requests measured approximately 1.7s for a 20-row scan and 4.2s for a 50-row scan. This is a functional smoke check, not a capacity benchmark; read-query latency remains a follow-up performance item before scaling. Exact-content reads in the smoke check took approximately 0.1–0.2s. All currently discoverable public boards were checked under both numeric and hosted IDs: their AppView external lanes returned no items, so this public sample had no external history requiring migration.

## iOS artifact — built and verified

`flutter build ipa --release --dart-define-from-file=config/production.json --export-options-plist=ios/ExportOptions-TestFlight.plist` completed successfully.

- Verified the final IPA's version, build number and bundle ID.
- `codesign --verify --deep --strict` passed.
- Distribution profile: Elix App Store Connect, team `K3X2X4CL3H`, development entitlement disabled.
- Generated Dart defines matched all production configuration values; the final compiled Dart binary contained production Relay/Issuer/Web URLs and no legacy AppView or development Relay endpoint.
- Release configuration tests: 8 passed. Shared identity-resolution contract passed.
- Artifact: `/private/tmp/elix-product-flow/ansible_node/app/build/ios/ipa/Elix.ipa`.

## TestFlight — awaiting local Apple login

The authorized Xcode export/upload attempt failed immediately with `exportArchive Failed to Use Accounts`. No successful Apple upload receipt exists for this build yet. The Mac was reported locked; the user has been asked to unlock it and confirm the Xcode publishing account is signed in. After login, retry the same verified archive with `ios/ExportOptions-TestFlight-Upload.plist`; do not increment the build number unless Apple reports that this build was already accepted.

Apple processing, tester assignment and installability are unverified. App Store review submissions were not changed.
