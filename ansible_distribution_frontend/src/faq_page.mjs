import { escapeHtml } from './forum_shell_renderer.mjs';
import { getCurrentLocale } from './web_i18n.mjs';

const COPY = Object.freeze({
  'zh-Hant': {
    kicker: '從這裡開始', title: 'Elix 是什麼？',
    intro: 'Elix 是一個讓人可以好好說話的社群 App。你不必先理解任何技術名詞；先把它當成一個可以閱讀、加入討論，也能自己決定資料怎麼流動的地方。',
    startTitle: '先記得這三件事就好',
    steps: [['先逛逛', '不登入也能閱讀公開看板與討論。'], ['想說話再登入', '登入後才能以自己的身分簽署發文；不需要一開始就完成所有設定。'], ['需要時再升級', '有些看板為了減少假帳號，才會要求更高的驗證層級。']],
    questions: [
      ['為什麼 Elix 要有「身分」？', '因為公開討論很容易被大量假帳號與自動化淹沒。Elix 讓你可以用自己的身分簽署內容，讓社群知道這則內容不是隨手可大量複製的匿名帳號產生的。這不等於必須公開真名。'],
      ['我一定要做真人驗證嗎？', '不用。閱讀公開內容與一般使用不以真人驗證為前提。只有個別看板選擇提高發文門檻時，才可能要求「已驗證真人」；看板應在你發文前清楚說明規則。'],
      ['驗證後，Elix 會拿到我的身分證或護照資料嗎？', '身分證號、護照號碼、姓名等原始個人資料，預設不會離開你的裝置，也不會被放進公開貼文或可分享的驗證資料。驗證時只會提出必要的證明，例如「這是一位已驗證的真人」；討論區不需要知道你是誰。'],
      ['我的貼文是私密的嗎？', '不是自動私密。公開或聯邦發布的內容會被視為可傳播；私密內容則必須在離開你的受信任範圍前受到保護。發送前請依畫面上的可見範圍選擇。'],
      ['為什麼需要同步？', '同步讓你可以在自己的裝置之間接續使用 Elix，例如換手機、在電腦上查看已追蹤的內容，或保留你已選擇發布的討論。是否同步、同步哪些資料，應由你決定；私密內容不會因為開啟同步就自動變成公開。'],
      ['我的資料保存在哪裡？', 'Elix 以你的裝置作為資料的主要保存位置。草稿、私密內容與身分相關資料預設留在你的載具上；只有你明確選擇發布、同步、備份或出示驗證時，才會依照該用途傳送必要資料。公開貼文則可能被你選擇的社群服務或其他公開網路節點保存與傳播。'],
      ['我看到發文者是 did:…，這是 bug 嗎？', '不是 bug。`did:…` 是 Elix 用來確認帳號與貼文歸屬的背景身分編號，有點像系統的帳號識別碼。它能讓身分不必綁死在單一網站或服務上，也能幫助確認貼文確實由該使用者發出。你不需要記住它；有顯示名稱或帳號名稱時，Elix 會優先顯示那些更好讀的資訊。'],
      ['貼文旁的簽章圖示是什麼？', '簽章圖示表示 Elix 已驗證這則貼文由顯示的身分簽署，內容與作者歸屬可以被檢查，也能發現事後竄改。它不代表對方使用真名、一定是好人，或 Elix 認同貼文內容；它只回答「這則內容是不是由這個身分發出」。沒有簽章圖示通常表示來源沒有提供可驗證的作者證明，或尚未通過驗證，並不等於內容一定是假的或違規。閱讀時仍請一起考量來源、看板規則與內容本身。'],
      ['誰決定我可以說什麼？', '每個看板可以訂自己的規則與發文門檻。Elix 的系統規則只處理安全、垃圾訊息、操弄、隱私與法律等高風險問題。你可以選擇、離開或封鎖看板與主機。'],
      ['我現在該做什麼？', '從一個你感興趣的公開看板開始。先讀，再決定要不要登入與參與；遇到需要驗證的看板，畫面會告訴你原因和下一步。'],
    ], nerdTitle: '想知道背後怎麼運作？', nerdIntro: '這一段給想研究技術架構的人；日常使用不需要記住它們。',
    nerds: [['Relay', '協助裝置連線、同步與探索。'], ['Forum Host', '負責一個討論區的規則與內容管理。'], ['AppView', '提供公開內容的閱讀視圖。']],
    nerdQuestions: [
      ['可以用 AI 協助我整理社群資料嗎？', '可以。在桌面版的「本地 AI 存取」中，你可以明確授權支援 MCP 的 AI 工具讀取指定的本機資料，協助整理已追蹤的內容、訂閱的看板或草稿。這個本地 MCP 存取預設為唯讀，沒有有效且未過期的授權就不會提供資料；錢包、私訊、原始身分資料與私鑰不在可存取範圍。若你使用的是會把內容傳到雲端的 AI 服務，仍須另外同意該服務的資料傳送。'],
      ['真人驗證怎麼兼顧隱私？', 'Elix 可以使用零知識證明等隱私保護技術，證明必要的事實，例如「這是一位真人」或符合某項資格，而不是交出護照號碼、身分證號或完整證件內容。例如在支援的 ePassport 驗證流程中，手機會在裝置上讀取護照晶片並產生 ZKPassport 證明；送出的只有可驗證的證明與必要結果，不是護照原始內容。證明也會綁定 Elix 的驗證用途，避免被拿到其他服務重複使用。系統與討論區只取得完成判斷所需的最少資訊。'],
      ['數位皮夾怎麼避免揭露太多資料？', '數位皮夾遵循最少揭露原則：驗證者只能請求完成當次判斷需要的聲明，例如年齡門檻或真人狀態。每次出示前，使用者應能看見要分享哪些聲明、交給誰與用途；原始身分資料與私鑰不會作為一般驗證資料送出。'],
      ['憑證只有年齡、國籍和真人驗證嗎？我或組織也能發嗎？', '不只這些。組織可以建立自己的憑證，例如「Elix 愛好者」、「活動參與者」或會員資格，並決定誰能簽發、有效期限與撤銷規則。個人也可以提出自我聲明，但它只代表本人這樣說，不能自動取得真人、年齡或會員等較高信任資格。憑證是否有作用，取決於看板或服務是否明確信任該發行者、憑證類型與聲明內容；Elix 不會因為一張憑證存在就自動給予特權。'],
    ], cta: '瀏覽公開看板',
  },
  en: {
    kicker: 'Start here', title: 'What is Elix?', intro: 'Elix is a social app for thoughtful conversation. You do not need to understand any technical terms first: begin by reading, then choose how you participate and where your data goes.', startTitle: 'Just remember these three things',
    steps: [['Browse first', 'Public boards and discussions are readable without signing in.'], ['Sign in when you want to speak', 'Signing in lets you sign your own posts; you do not need to complete every setup step first.'], ['Upgrade only when needed', 'Some boards use higher assurance to reduce fake accounts.']],
    questions: [['Why does Elix use identity?', 'It helps communities resist cheap fake accounts and automation. Signing a post shows it came from your identity, without requiring you to publish your real name.'], ['Do I have to verify that I am human?', 'No. Public reading and ordinary use do not require it. A board may ask for verified-human status before posting, and should explain that rule before you post.'], ['Does verification give Elix my ID or passport details?', 'By default, raw identity details are not put in public posts or shareable verification data. Verification should reveal only the fact needed, such as verified-human status.'], ['Are my posts private?', 'Not automatically. Public or federated content can spread; private content must be protected before it leaves your trusted boundary. Check the visibility setting before sending.'], ['Why is sync needed?', 'Sync lets you continue using Elix across your own devices: for example, after changing phones, viewing followed content on a computer, or retaining discussions you chose to publish. You decide whether to sync and what to sync; enabling sync does not automatically make private content public.'], ['Where is my data stored?', 'Elix uses your device as the primary place to store data. Drafts, private content, and identity-related data stay on your device by default; only when you explicitly choose to publish, sync, back up, or present a verification does necessary data move for that purpose. Public posts can be retained and distributed by the community services or public network nodes you choose.'], ['I see an author named did:… — is that a bug?', 'No. A `did:…` value is the background identity identifier Elix uses to confirm account and post ownership, much like a system account ID. It keeps an identity from being tied to one website or service and helps verify that a post was made by that user. You do not need to remember it: when a display name or handle is available, Elix shows that easier-to-read information first.'], ['What does the signature icon next to a post mean?', 'The signature icon means Elix verified that the post was signed by the displayed identity. Its content and authorship can be checked, and later tampering can be detected. It does not mean the person uses their real name, is necessarily trustworthy, or that Elix endorses the post; it answers only whether this identity made it. No signature icon usually means the source did not provide verifiable authorship or verification did not succeed. It does not by itself mean the post is false or against the rules: also consider its source, the board rules, and the content itself.'], ['Who decides what I can say?', 'Each board can set its own rules and posting requirements. System-wide rules are limited to high-risk concerns such as safety, spam, manipulation, privacy, and legal obligations.'], ['What should I do now?', 'Start with a public board that interests you. Read first, then decide whether to sign in and join the conversation.']], nerdTitle: 'Want the technical version?', nerdIntro: 'This part is for people curious about the architecture; you do not need it for everyday use.', nerds: [['Relay', 'Helps devices connect, sync, and discover content.'], ['Forum Host', 'Runs a discussion space and its rules.'], ['AppView', 'Provides a reading view for public content.']], nerdQuestions: [['Can AI help me organize community information?', 'Yes. In desktop Settings → Local AI Access, you can explicitly grant an MCP-capable AI tool access to selected local data to organize followed content, subscribed boards, or drafts. This local MCP access is read-only and returns no data without a valid, unexpired grant; wallet data, direct messages, raw identity data, and private keys are excluded. If you use an AI service that sends content to the cloud, you must separately consent to that transfer.'], ['How can verified-human status protect privacy?', 'Elix can use privacy-preserving techniques such as zero-knowledge proofs to prove a necessary fact, such as verified-human status or eligibility, without handing over a passport number, national ID number, or complete document. For example, in supported ePassport verification flows, the phone reads the passport chip and creates a ZKPassport proof on-device; it sends a verifiable proof and necessary result, not the raw passport contents. The proof is bound to Elix’s verification purpose so it cannot simply be reused by another service. The system and discussion space receive only the minimum information needed for the decision.'], ['How does the digital wallet avoid revealing too much?', 'The wallet follows data minimization: a verifier can request only the claims needed for a decision, such as an age threshold or verified-human status. Before each presentation, the user should see which claims are shared, with whom, and why. Raw identity data and private keys are not sent as ordinary verification data.'], ['Are credentials limited to age, nationality, and verified-human status? Can I or an organization issue one?', 'No. Organizations can create their own credentials, such as an Elix enthusiast, event attendee, or membership credential, and define who may issue them, how long they remain valid, and how they are revoked. A person can also make a self-attestation, but it only says that person makes the claim; it cannot automatically grant higher-assurance status such as verified-human, age, or membership. A credential has an effect only when a board or service explicitly trusts its issuer, type, and claims. Elix does not grant privileges just because a credential exists.']], cta: 'Browse public boards',
  },
});

export function renderElixFaq() {
  const copy = COPY[getCurrentLocale()] ?? COPY['zh-Hant'];
  return `<section class="faq-page" aria-labelledby="faq-title">
    <header class="faq-hero"><p class="section-label">${escapeHtml(copy.kicker)}</p><h1 id="faq-title">${escapeHtml(copy.title)}</h1><p>${escapeHtml(copy.intro)}</p><a class="primary-action" href="#/boards">${escapeHtml(copy.cta)}</a></header>
    <section class="faq-start" aria-labelledby="faq-start-title"><h2 id="faq-start-title">${escapeHtml(copy.startTitle)}</h2><ol>${copy.steps.map(([title, text]) => `<li><strong>${escapeHtml(title)}</strong><span>${escapeHtml(text)}</span></li>`).join('')}</ol></section>
    <section class="faq-list" aria-label="Frequently asked questions">${copy.questions.map(([question, answer]) => `<details><summary>${escapeHtml(question)}</summary><p>${escapeHtml(answer)}</p></details>`).join('')}</section>
    <details class="faq-nerds"><summary>${escapeHtml(copy.nerdTitle)}</summary><p>${escapeHtml(copy.nerdIntro)}</p><dl>${copy.nerds.map(([term, description]) => `<div><dt>${escapeHtml(term)}</dt><dd>${escapeHtml(description)}</dd></div>`).join('')}</dl>${copy.nerdQuestions.map(([question, answer]) => `<details class="faq-nerd-question"><summary>${escapeHtml(question)}</summary><p>${escapeHtml(answer)}</p></details>`).join('')}</details>
  </section>`;
}
