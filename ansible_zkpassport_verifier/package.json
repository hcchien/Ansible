{
  "name": "ansible-zkpassport-verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/server.js --bundle --packages=external --platform=node --format=cjs --target=node22 --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "test": "node --test"
  },
  "dependencies": {
    "@aztec/bb.js": "5.0.0",
    "@zkpassport/sdk": "0.16.1",
    "@zkpassport/utils": "0.37.3"
  },
  "devDependencies": {
    "esbuild": "0.27.4"
  }
}
