{
  "name": "ansible-zkpassport-verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/server.js --bundle --platform=node --format=esm --target=node22 --outfile=dist/server.js",
    "start": "node dist/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@zkpassport/sdk": "0.16.1"
  },
  "devDependencies": {
    "esbuild": "0.27.4"
  }
}
