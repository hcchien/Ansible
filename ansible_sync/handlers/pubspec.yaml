name: ansible_sync
description: Sync handlers for Ansible ActivityPub and follow inbox delivery.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.10.0

dependencies:
  ansible_ap:
    path: ../../ansible_core/ap
  ansible_domain:
    path: ../../ansible_core/domain
  ansible_store:
    path: ../../ansible_core/store
  shelf: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.21.0
