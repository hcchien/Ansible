name: ansible_sync
description: HTTP /inbox + /sync/delta handlers powered by ansible_domain
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.10.0

dependencies:
  ansible_domain:
    path: ../../ansible_core/domain
  ansible_store:
    path: ../../ansible_core/store
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.21.0
